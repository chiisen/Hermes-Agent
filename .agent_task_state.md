# Agent Task State

🚩 目標: 沉澱 Hermes Agent MiniMax API 404 排查方法
✅ 進展:
- 已定位根因為 MiniMax base URL 被覆蓋成 `https://api.minimax.io/v1`
- 已修正 `~/.hermes/.env` 與 `~/.hermes/auth.json` 為 `https://api.minimax.io/anthropic`
- 已用 `hermes doctor` 與 `hermes -z` 驗證 MiniMax 連線及對話成功
- 已新增 `docs/MINIMAX_404_RUNBOOK.md` 作為下次快速排查文件
🚀 下一步: 建議輪換剛暴露於診斷輸出的 MiniMax API Key
