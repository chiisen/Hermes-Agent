services:
    laravel.test:
        build:
            context: "./docker/8.4"
            dockerfile: Dockerfile
            args:
                WWWGROUP: "${WWWGROUP}"
        image: "sail-8.4/app"
        extra_hosts:
            - "host.docker.internal:host-gateway"
        ports:
            - "${FORWARD_HTTP_PORT:-80}:80"
            - "${VITE_PORT:-5173}:${VITE_PORT:-5173}"
            - "${FORWARD_REVERB_PORT:-7080}:7080"
        environment:
            WWWUSER: "${WWWUSER}"
            DB_HOST: "${DB_HOST}"
            LARAVEL_SAIL: 1
            XDEBUG_MODE: "${SAIL_XDEBUG_MODE:-off}"
            XDEBUG_CONFIG: "${SAIL_XDEBUG_CONFIG:-client_host=host.docker.internal}"
            IGNITION_LOCAL_SITES_PATH: "${PWD}"
            APP_COLOR: "dev"
            DB_QUEUE: "default_dev"
            REDIS_QUEUE: "default_dev"
        volumes:
            - ".:/var/www/html"
            - "../docker/assets/gh-ost:/usr/local/bin/gh-ost"
        networks:
            - sail-jubilant
        depends_on:
            - mysql
            - redis
    phpmyadmin:
        image: phpmyadmin/phpmyadmin
        restart: always
        ports:
            - "${FORWARD_PHPMYADMIN_PORT:-3307}:80"
        environment:
            MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
            PMA_HOST: mysql
            PMA_USER: root
            PMA_PASSWORD: ${DB_PASSWORD}
        networks:
            - sail-jubilant
    mysql:
        image: "mysql/mysql-server:8.0"
        ports:
            - "${FORWARD_DB_PORT:-3306}:3306"
        environment:
            MYSQL_ROOT_PASSWORD: "${DB_PASSWORD}"
            MYSQL_ROOT_HOST: "%"
            MYSQL_DATABASE: "${DB_DATABASE}"
            MYSQL_USER: "${DB_USERNAME}"
            MYSQL_PASSWORD: "${DB_PASSWORD}"
            MYSQL_ALLOW_EMPTY_PASSWORD: 1
        volumes:
            - "sail-jubilant-mysql:/var/lib/mysql"
            - "./docker/mysql/create-testing-database.sh:/docker-entrypoint-initdb.d/10-create-testing-database.sh"
        networks:
            - sail-jubilant
        healthcheck:
            test:
                - CMD
                - mysqladmin
                - ping
                - "-p${DB_PASSWORD}"
            retries: 3
            timeout: 5s
    redis:
        image: "redis:alpine"
        ports:
            - "${FORWARD_REDIS_PORT:-6379}:6379"
        volumes:
            - "sail-jubilant-redis:/data"
        networks:
            - sail-jubilant
        healthcheck:
            test:
                - CMD
                - redis-cli
                - ping
            retries: 3
            timeout: 5s
    redis-insight:
        image: redis/redisinsight:latest
        container_name: "${REDIS_INSIGHT_CONTAINER_NAME:-redis-insight}"
        restart: unless-stopped
        profiles:
            - redis-insight
        ports:
            - "${FORWARD_REDIS_INSIGHT_PORT:-5540}:5540"
        volumes:
            - sail-jubilant-redis-insight:/data
        networks:
            - sail-jubilant
networks:
    sail-jubilant:
        driver: bridge
volumes:
    sail-jubilant-mysql:
        driver: local
    sail-jubilant-redis:
        driver: local
    sail-jubilant-redis-insight:
        driver: local
